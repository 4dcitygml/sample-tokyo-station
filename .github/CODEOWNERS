# Every change requires the maintainer's review (branch rule "Require review
# from Code Owners"). This is enforced by GitHub itself and cannot be bypassed
# by forged CI results or artifacts.
#
# The personal maintainer account is the code owner; the organization itself
# cannot be used as a code owner. The former owner-less exception for the
# practice data files (auto-merge of data-only PRs) was removed on 2026-08-24;
# reinstate it only together with a decision to resume automatic merging.

*   @citydatawalker
